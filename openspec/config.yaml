strict_tdd: true
context: |
  apps-marketing is a Node.js/TypeScript, React, Next.js project.
  Detected markers: package.json, tsconfig.json, npm package manager.
  Package managers: npm.
  Additional evidence: Test-like files detected (32); examples: .pi/agents/sdd-spec.md, docs/01-sdd/lead-scoring-spec.md, src/app/api/admin/leads/[id]/conversation/route.test.ts, src/app/api/admin/leads/[id]/conversation/suggestion/route.test.ts, src/app/api/admin/leads/[id]/notes/route.test.ts.
  Primary test command: npm test.
  Unit tests: Vitest (npm test).
  Integration tests: none.
  E2E tests: none.
rules:
  proposal:
    require_problem_statement: true
  spec:
    require_acceptance_criteria: true
  design:
    require_tradeoffs: true
  tasks:
    protect_review_workload: true
  apply:
    test_command: "npm test"
  verify:
    test_command: "npm test"
testing:
  detected: "2026-05-16"
  runner:
    command: "npm test"
    framework: "Vitest"
  layers:
    unit: "Vitest"
    integration: ""
    e2e: ""
  commands:
    unit:
      - scope: "."
        command: "npm test"
        framework: "Vitest"
    integration:
      []
    e2e:
      []
  coverage:
    command: ""
    commands:
      []
quality:
  lint: "npm run lint"
  lint_commands:
    - scope: "."
      command: "npm run lint"
      framework: "linter"
  typecheck: ""
  typecheck_commands:
    []
  format: ""
  format_commands:
    []
